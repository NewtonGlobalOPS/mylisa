# Optional: set VITE_API_BASE only if the API is on a different host/port.
# If omitted, the frontend will call the current page hostname on port 4010.
# VITE_API_BASE=http://192.168.1.41:4010
VITE_API_KEY=replace-with-your-mylisa-api-key
VITE_GOOGLE_CLIENT_ID=google-web-client-id.apps.googleusercontent.com
